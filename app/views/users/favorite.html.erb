<h1><%= @user.name %>がいいねした投稿一覧</h1>

<% if @favorite_posts.present? %>
  <ul>
    <% @favorite_posts.each do |post| %>
      <li>
        <%= link_to post.title, post %>
        (投稿者: <%= link_to post.user.name, user_path(post.user) %>)
      </li>
    <% end %>
  </ul>
<% else %>
  <p>いいねした投稿はありません。</p>
<% end %>