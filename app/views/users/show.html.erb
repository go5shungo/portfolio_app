<div>
  <h3><%= link_to @user.name, edit_user_path %>のマイページ</h3>                   <%# ユーザーの名前クリックしたら編集できるようにしたい。名前とパスワード、メールアドレス等。退会もこのリンクにつくりたい %>
  
  <%= link_to "MYペット", pets_path %>
        <div class="col-7">
          <%= image_tag @user.get_profile_image(200,200), class: "mt-7"; %>
        </div>
</div>

<div class="card-list-container py-5">
  <% @posts.each do |post| %>
  <div class="card w-50 mx-auto mt-4 shadow-lg">
    <%= link_to post_path(post.id) do %>
      <%= image_tag post.image, class: "card-img-top"; %>
    <% end %>
    <div class="card-body">
      <div class="row">
        
          <div class="col-10">
           <p class="card-title">
             <%= link_to(post_path(post.id), class: "text-dark") do %>
               <strong><i class="farimage fa-flag"></i><span> <%= post.title %></span></strong>
             <% end %>
           </p>
           <p class="card-text">本文：<%= post.body %></p>
            <span><i class="fas fa-user"></i> By <%= post.user.name %> ｜ </span>
           <p><%= link_to "#{post.post_comments.count} コメント", post_path(post.id) %></p>
          </div>
      </div>
    </div>
  </div>
  <% end %>

</div>
  
